# 策略文档 vs 当前实现 — 差异分析 (2026-05-27)

对比 `尾盘策略0514.txt` 完整 7 阶段策略设计与当前代码实现。

---

## S0: 板块预筛选

### 一致
- S0 存在，按配置板块 → 成分股 → 基础准入流程
- `s0_min_stocks=200` 下限已生效

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **动态 Top 3 板块**（按涨幅/成交额/资金流向实时排名） | `.env TARGET_SECTORS` 静态配置，手动指定板块 | **高** |
| 板块数量参数 `s0_top_n=3`, `s0_max_n=5` | 已定义（`config.py:130-131`）但 **`sector_filter.py` 完全未引用**，死代码 | **高** |
| 输出 200-500 只，有上限控制 | 仅下限 200，无上限；多板块可超过 500 | 中 |
| 14:25-14:30 时间窗口 | 无时间检查，S0 调用即执行 | 低 |

**文件**: `data_provider/sector_filter.py:24-61` (静态映射), `orchestration/config.py:130-131` (死代码)

---

## S1: K线形态预筛选 + L1 准入

### 一致
- L1 基础准入: ST/停牌/一字板排除, 成交额>50M, 换手>1%, 股价≥5
- K线 R1 7项检查完全匹配: ATR范围/连涨/涨频/阳线比/收盘动量/单日涨幅/涨幅vsATR
- K线 R2 6项检查完全匹配: 涨幅不骤降/不连续递减/实体不缩小/突破前高/收盘>开盘/上影线
- R2 不淘汰仅警告（正确）
- R2 在 S1 (K线阶段) 而非 S2（正确）

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **S1 14:30-14:50 每3分钟循环扫描** | `_run_stage1()` 单次执行，无 while 循环、无 `_sleep_or_break` | **高** |
| `s1_loop_interval=180` | 已定义（`config.py:150`）但 **`pipeline.py` 完全未引用**，死代码 | **高** |
| R1(14:30) 和 R2(14:33/14:36) 分两次扫描 | R1+R2 在同一次 `screen_kline()` 调用中顺序执行 | 中 |
| 连续4天收盘涨 ≥0.5% | 实际为 `min_close_rise_pct=0.3%`，阈值降低 | 低 |
| 今日必须阳线 | R1 第7项(pct_vs_atr)不检查今日阴阳，仅检查涨跌幅 vs ATR | 低 |

**文件**: `orchestration/pipeline.py:576-598` (单次执行), `screening/layer_kline.py:92-109` (R1+R2同次)

---

## S2: 尾盘异动精选

### 一致
- 时间窗口 14:50-14:55, 循环间隔 60s
- 尾盘拉升>2%, 企稳形态, 突破日内高点
- 大单净流入>0, 尾盘大单占比>1.3x, 主动买入>55%
- 撤单率<30%

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **量比 午后总量/上午总量 >1.5** | `late_volume_ratio = (14:30后每bar均值)/(13:00-14:30每bar均值) ≥1.2`，定义和阈值都不同 | **高** |
| **最后5分钟量占比 >8%** | `last_5min_vol_pct_min = 5.0%` | **高** |
| **盘口检查（买盘>卖盘）** | `require_orderbook=False`，默认关闭 | 中 |
| 突破日内高点需**伴随放量** | 仅 `broke_high` 即通过，无附加量条件 | 中 |
| 输出目标 50-80 只 | 代码注释目标 100-200 只 | 中 |
| 资金门 min_pass 回退 | 通过<10时完全移除资金条件（已知 Problem 7） | 中 |

**文件**: `screening/layer2_anomaly.py:21-22` (阈值), `data_provider/kline_provider.py:395-399` (量比定义)

---

## S3: 均线系统验证

### 一致
- 时间窗口 14:55-14:57, 循环间隔 30s
- 多头排列检查 (MA5≥MA10 + 收盘>MA20>MA30>MA60)
- 收盘站稳MA5, 最低价不破MA5
- 不允许连续3天缩量>10%
- 板块涨幅前30%, 概念热度, 龙头效应
- 利空公告排查, 解禁日检查
- 输出目标 30-50 只

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **MA5加速 0.4%→0.2%→0.1%（3天三档）** | `rate1≥0.1% and rate2≥0.05%`（2天两档），仅用于L4评分非L3硬筛 | **高** |
| **近4天量比 1.3~1.8** | `vol_ratio` 为盘中实时量比(当日/5日均)，非近4天概念 | **高** |
| **vol_ratio范围 1.3~1.8** | 当前 `[1.1, 2.0]`，范围更宽 | 中 |
| **突破平台/颈线位检测** | 仅整数关口±1% + MA20/MA60±2%，无平台/颈线模式识别 | 中 |
| **业绩预亏检查** | 完全缺失 | 中 |
| position_20d "前50%" | 实现为 `(close-low_20d)/(high_20d-low_20d)≤50`（区间下半部），策略可能指"按20日涨幅排名前50%" | 低 |

**文件**: `screening/layer3_technical.py:26-27` (量比范围), `data_provider/kline_provider.py:228` (MA5加速)

---

## S4: 融合量化评分

### 一致
- 5维度 100 分制: A尾盘(30), B K线(25), C资金(20), D均线(15), E市场(10)
- 时间窗口 14:57-14:58, 循环间隔 10s

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **固定阈值 >85/75-85/60-75/<60** | 自适应阈值（4套方案，最低 65/55/40），根据资金流+LLM可用性动态调整 | **高** |
| **纯规则评分** | 规则(70%) + LLM(30%) 融合，LLM 仅首轮执行 | 中 |
| **T+0买入 (3-5只) / T+1观察 (5-8只)** | 推荐标签 `strong_buy`/`buy`/`watch`，无 T+0/T+1 区分，无数目限制 | 中 |
| 评分 >85 超强信号→立即买入 | 最高阈值 80（全数据场景），比策略低 5 分 | 低 |

**文件**: `orchestration/pipeline.py:921-940` (自适应阈值), `analysis/merger.py:19-89` (LLM融合)

---

## S5: 下单执行 — **完全缺失**

策略要求 14:58-14:59:30，每5秒一次：
- 下单前最后验证（K线/均线/资金/盘口）
- 仓位管理：90+分→10%仓, 85-90→8%, <85→6%
- 单只不超 10% 总仓位
- 订单执行监控

`pipeline.py:9` 明确标注 "S5-S7 后续实现"。

---

## S6: 收盘确认 — **完全缺失**

策略要求 14:59:30-15:00：
- 收盘价与买入价偏差
- K线形态是否符合预期
- 均线支撑是否有效
- 资金流向最终状态

---

## S7: 报告生成

### 一致
- Markdown 报告输出
- 个股基础信息 + 5维度评分明细
- 大盘指数（上证/深证/创业板）+ 北向资金（已实现于 5/27）
- 市场宽度涨跌家数（已实现于 5/27）
- 热点板块 Top5（已实现于 5/27）

### 差异

| 策略要求 | 当前实现 | 严重度 |
|---------|---------|--------|
| **报告 15:00-15:05 生成** | S4 结束后立即生成（~14:58），无 S7 时间窗口 | 中 |
| **K线形态分析专节** | 仅有评分维度表格，无独立分析 | 中 |
| **均线系统分析专节** | 同上 | 中 |
| **风险提示专节** (K线/均线/资金/板块风险等级) | 仅有数据质量声明 | 中 |
| **次日操作建议** (开盘准备/持仓决策/T+1入场条件/放弃条件) | 完全缺失 | 中 |
| **执行日志** (下单时间/成交/偏差/异常) | 依赖 S5/S6，全部缺失 | 中 |
| **板块详情** (成交额/资金流向/龙头股/入选数) | 仅有板块名+涨幅 | 低 |
| **资金流向分析专节** (主力/大单/中单/小单/北向) | 仅单行北向+数据日期 | 低 |
| 尾盘特征分析 (拉升/跳水/放量股票数) | 缺失 | 低 |
| stock_card.j2 个股卡片 | 已实现但 `_generate_report()` 未调用 | 低 |
| HTML/PDF/PNG 多格式输出 | 仅 Markdown（`md_to_image` 可选） | 低 |

**文件**: `report/templates/report.j2`, `orchestration/pipeline.py:1027-1091`

---

## Dead Code 汇总

| 配置项 | 定义位置 | 状态 |
|--------|---------|------|
| `s0_top_n: int = 3` | `config.py:130` | `sector_filter.py` 未引用 |
| `s0_max_n: int = 5` | `config.py:131` | `sector_filter.py` 未引用 |
| `s1_loop_interval: int = 180` | `config.py:150` | `pipeline.py` 未引用 |
| `schedule_enabled: bool = False` | `config.py:156` | `pipeline.py` 未引用 |
| `stock_card.j2` | `report/templates/` | `_generate_report()` 未调用 |

---

## 严重度统计

| 级别 | 数量 | 涉及阶段 |
|------|------|---------|
| **高** | 9 | S0(2), S1(2), S2(2), S3(2), S4(1) |
| **中** | 18 | S0-S7 均有分布 |
| **低** | 8 | S1-S7 均有分布 |
| **完全缺失** | 2 阶段 | S5, S6 |

---

## 优先级建议

1. **P0 — 立即**: S2 量比定义修正（从每bar均比改为午后/上午总量比）、S2 尾盘量占比 5%→8%
2. **P0 — 立即**: S1 改为循环扫描（启用 `s1_loop_interval`）
3. **P1 — 近期**: S0 动态板块排名（启用量价排名替代静态配置）
4. **P1 — 近期**: S3 MA5加速三档、近4天量比、vol_ratio 范围收紧
5. **P2 — 计划**: S4 固定阈值回归策略定义
6. **P3 — 远期**: S5/S6 下单执行+收盘确认
7. **P3 — 远期**: 报告增强（风险/次日建议/板块详情/执行日志）
