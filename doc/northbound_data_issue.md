# 北向资金数据问题 — 待办

## 问题确认 (2026-05-27)

同花硕 `dayChart` API 返回的是**上一完整交易日**数据，非当日实时：

- 盘中(9:30AM)调用 → 返回262个数据点(09:10~15:00)，末值=15:00
- 连续3天输出完全相同的值(-40.38亿, 趋势分10)
- 东方财富全系北向API自2024-08后断供（净买额字段返回NaN/0）

## 影响范围

### 已修复 (标注"昨日")
- 实盘日志输出: `orchestration/pipeline.py` ~line 853
- 报告模板: `report/templates/report.j2` ~line 104-107
- 数据模块注释: `data_provider/northbound_fetcher.py`

### 评分影响 (低风险)
- L4评分C维度: 北向是加分项(最多+3分)，无北向不扣分，影响可控
- 配置: `L4_NORTHBOUND_TIERS: [[70,3],[55,1]]`

### 待决策 (高风险)
- `doc/market_regime_dual_threshold.md` — 牛熊判断因子1依赖北向趋势分
  - 用T+1北向做牛熊判断是否可接受？
  - 是否需要替换数据源？

### 不受影响
- 回测系统: 已使用akshare历史日度汇总(趋势分固定50)，本就有滞后

## 备选方案

1. 接受T+1延迟 → 当前已实施(标注"昨日")
2. 换数据源 → 东方财富已断供，需另找实时北向API
3. 降级北向权重 → 将L4北向分从3分降到1分，降低对滞后数据的依赖
